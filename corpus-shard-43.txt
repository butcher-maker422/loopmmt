# Loop MMT — corpus shard 43/91

> Bundle 43 of 91 in the excavation shard set. The authoritative tree is https://loopmmt.com/corpus-manifest.json; the shard index is https://loopmmt.com/corpus-shards.json. Read every shard (or account for each skipped node by coverage class) to complete a full excavation. See The Reckoning for the coverage-ledger and final-report contract.

Nodes in this shard: 7 (/site/schematics/photoshop-killer.html, /site/schematics/spreadsheet.html, /site/schematics/sudoku.html, /site/schematics/text-editor.html, /site/schematics/timeline.html, /site/search.html …)

---

# Photoshop Killer · Schematics · Loop MMT
https://loopmmt.com/site/schematics/photoshop-killer.html
[coverage: standard · branch: schematics]

Graphics · S-04 · 3 interactive lanes

∘ ∘ ∘ ∘ ∘ ∘ ∘ ⊗

Photoshop Killer

Layered image editing, wired from convolution and compositing.

We have not built this app. This is a
schematic — the compiler’s reading of how a batch of gifts would
compose into a whole app, with the wiring and the honest gaps laid out. There is
no download of the finished app, no repo behind this page, and we make no claim
to have made one.

The wiring below is a proof you can read, check, and
argue with — and the gifts it names are real and free. It’s here to show
what compiles, not to sell you a product.

The wiring ∘ feeds   ⊗ parallel

This is the compiler’s reading of how the gifts compose into one app. Read left to right: each stage feeds the next ( ∘). A bracketed group ( ⊗) is parallel — its parts don’t depend on each other.

decode ⊗ metadata read

∘

crop mask

∘

pixel transform

∘

kernel convolve

∘

layer composite

∘

encode

Composition term: encode ∘ layer-composite ∘ kernel-convolve ∘ pixel-transform ∘ crop-mask ∘ (decode ⊗ metadata-read)

⊗ marks a parallel-independent group: decode, metadata read run in either order — neither consumes the other’s output. both read the same source file, neither consumes the other's output; re-ordering is order-independent (a trivial braid), so ⊗ is earned per the falsifier. This is the ONLY parallel join in the batch core (recipe E.1).

◈ Compiles with declared gaps

2 declared gaps — named below, not hidden.

CONSTRUCTIBLE-WITH-2-NAMED-GAPS

The declared gaps 2

A gap is a piece the compiler knows it needs but doesn’t yet have a witnessed gift for — honest derive-first work, declared rather than hidden. The app is constructible; these are the parts you’d build.

pk.kernel-convolve pure-kernel

null-witness pure-kernel (no gift in the roster witnesses it); derive-first, gifts-8-reference-apps line work. Declared, not hidden.

pk.layer-composite pure-kernel

null-witness pure-kernel (fold N layer-rasters under a blend fn); derive-first. Declared, not hidden.

Witness gifts

No pipe stage in this schematic is witnessed by a shipped gift yet — the wiring is the compiler’s declared plan, and the witnessing gifts are still to come. Declared, not hidden.

The gifts are real and free; the wiring is a plan. Take the parts and build
the app — as the schematic reads it, a variation, or something we’d never
have compiled.

Built it, found a gap we missed, or think the wiring is
wrong? Shea wants to hear it — success stories, questions, and corrections all
land in the same inbox:

shea@loopmmt.com

---

# Spreadsheet · Schematics · Loop MMT
https://loopmmt.com/site/schematics/spreadsheet.html
[coverage: standard · branch: schematics]

Productivity · S-05 · 1 interactive lane

∘ ∘ ∘ ∘ ∘ ∘ ∘ ⊗

Spreadsheet

A working spreadsheet, wired from small pieces.

We have not built this app. This is a
schematic — the compiler’s reading of how a batch of gifts would
compose into a whole app, with the wiring and the honest gaps laid out. There is
no download of the finished app, no repo behind this page, and we make no claim
to have made one.

The wiring below is a proof you can read, check, and
argue with — and the gifts it names are real and free. It’s here to show
what compiles, not to sell you a product.

The wiring ∘ feeds   ⊗ parallel

This is the compiler’s reading of how the gifts compose into one app. Read left to right: each stage feeds the next ( ∘). A bracketed group ( ⊗) is parallel — its parts don’t depend on each other.

grid model

∘

render grid

Composition term: render-grid ∘ grid-model

◈ Compiles with declared gaps

3 declared gaps — named below, not hidden.

constructible-with-gaps: 2 pipe primitives fully witnessed (grid-source, grid-sink); 3 declared pure-kernel gaps (formula-parse, dep-graph, recalc) — the spreadsheet compute core is honest derive-first work. One interactive sub-lane (edit-cell / input-handling).

The declared gaps 3

A gap is a piece the compiler knows it needs but doesn’t yet have a witnessed gift for — honest derive-first work, declared rather than hidden. The app is constructible; these are the parts you’d build.

ss.formula-parse pure-kernel

text -> formula AST: a deterministic parse, a pure fold. Reclassified pipe->pure-kernel by the S13.1752 D1 ruling (drift 8251ecec). Declared derive-first gap; a formula-parser witness gift (path b) is open gifts-8-reference-apps work.

ss.dep-graph pure-kernel

cells -> dependency DAG: a deterministic fold. Declared derive-first gap (oracle pure-kernel, null-witness).

ss.recalc pure-kernel

dep-graph + values -> recomputed values: a deterministic topological fold. Declared derive-first gap (oracle pure-kernel, null-witness).

Witness gifts 2

These shipped gifts witness stages of the pipeline — the real, downloadable parts the proof stands on.

grid-source

grid-sink

The gifts are real and free; the wiring is a plan. Take the parts and build
the app — as the schematic reads it, a variation, or something we’d never
have compiled.

Built it, found a gap we missed, or think the wiring is
wrong? Shea wants to hear it — success stories, questions, and corrections all
land in the same inbox:

shea@loopmmt.com

---

# Sudoku · Schematics · Loop MMT
https://loopmmt.com/site/schematics/sudoku.html
[coverage: standard · branch: schematics]

Game · S-06 · 1 interactive lane

∘ ∘ ∘ ∘ ∘ ∘ ∘ ⊗

Sudoku

A playable sudoku, compiled from shipped gifts.

We have not built this app. This is a
schematic — the compiler’s reading of how a batch of gifts would
compose into a whole app, with the wiring and the honest gaps laid out. There is
no download of the finished app, no repo behind this page, and we make no claim
to have made one.

The wiring below is a proof you can read, check, and
argue with — and the gifts it names are real and free. It’s here to show
what compiles, not to sell you a product.

The wiring ∘ feeds   ⊗ parallel

This is the compiler’s reading of how the gifts compose into one app. Read left to right: each stage feeds the next ( ∘). A bracketed group ( ⊗) is parallel — its parts don’t depend on each other.

grid model

∘

constraint check

∘

render grid

Composition term: render-grid ∘ constraint-check ∘ grid-model

◈ Compiles with declared gaps

2 declared gaps — named below, not hidden.

CONSTRUCTIBLE-WITH-2-NAMED-GAPS

The declared gaps 2

A gap is a piece the compiler knows it needs but doesn’t yet have a witnessed gift for — honest derive-first work, declared rather than hidden. The app is constructible; these are the parts you’d build.

sudoku.solve pure-kernel

null-witness pure-kernel (constraint-propagation/backtracking fold to a solved grid); derive-first. Declared, not hidden.

sudoku.generate pure-kernel

null-witness pure-kernel (seeded puzzle generator, source-role); derive-first. Declared, not hidden. NOTE: a source-role kernel -- tests the verifier/compiler handle a gap that is not a fold.

Witness gifts 3

These shipped gifts witness stages of the pipeline — the real, downloadable parts the proof stands on.

seq-source

schema-filter

grid-sink

The gifts are real and free; the wiring is a plan. Take the parts and build
the app — as the schematic reads it, a variation, or something we’d never
have compiled.

Built it, found a gap we missed, or think the wiring is
wrong? Shea wants to hear it — success stories, questions, and corrections all
land in the same inbox:

shea@loopmmt.com

---

# Text Editor · Schematics · Loop MMT
https://loopmmt.com/site/schematics/text-editor.html
[coverage: standard · branch: schematics]

Productivity · S-07 · 3 interactive lanes

∘ ∘ ∘ ∘ ∘ ∘ ∘ ⊗

Text Editor

A real text editor — buffer, highlight, find-replace — compiled.

We have not built this app. This is a
schematic — the compiler’s reading of how a batch of gifts would
compose into a whole app, with the wiring and the honest gaps laid out. There is
no download of the finished app, no repo behind this page, and we make no claim
to have made one.

The wiring below is a proof you can read, check, and
argue with — and the gifts it names are real and free. It’s here to show
what compiles, not to sell you a product.

The wiring ∘ feeds   ⊗ parallel

This is the compiler’s reading of how the gifts compose into one app. Read left to right: each stage feeds the next ( ∘). A bracketed group ( ⊗) is parallel — its parts don’t depend on each other.

buffer load

∘

find replace

∘

syntax highlight

∘

buffer save

Composition term: buffer-save ∘ syntax-highlight ∘ find-replace ∘ buffer-load

◆ Compiles complete

CONSTRUCTIBLE-COMPLETE

Witness gifts 4

These shipped gifts witness stages of the pipeline — the real, downloadable parts the proof stands on.

line-source

rename

legible

l21x-snapshot

The gifts are real and free; the wiring is a plan. Take the parts and build
the app — as the schematic reads it, a variation, or something we’d never
have compiled.

Built it, found a gap we missed, or think the wiring is
wrong? Shea wants to hear it — success stories, questions, and corrections all
land in the same inbox:

shea@loopmmt.com

---

# Timeline · Schematics · Loop MMT
https://loopmmt.com/site/schematics/timeline.html
[coverage: core · branch: schematics]

Productivity · S-08 · 1 interactive lane

∘ ∘ ∘ ∘ ∘ ∘ ∘ ⊗

Timeline

A filterable timeline, compiled from axis + window + event model.

We have not built this app. This is a
schematic — the compiler’s reading of how a batch of gifts would
compose into a whole app, with the wiring and the honest gaps laid out. There is
no download of the finished app, no repo behind this page, and we make no claim
to have made one.

The wiring below is a proof you can read, check, and
argue with — and the gifts it names are real and free. It’s here to show
what compiles, not to sell you a product.

The wiring ∘ feeds   ⊗ parallel

This is the compiler’s reading of how the gifts compose into one app. Read left to right: each stage feeds the next ( ∘). A bracketed group ( ⊗) is parallel — its parts don’t depend on each other.

event model

∘

window filter

∘

render axis

Composition term: render-axis ∘ window-filter ∘ event-model

◈ Compiles with declared gaps

1 declared gap — named below, not hidden.

constructible-with-gap: 3 pipe primitives fully witnessed (timeline, range-filter, gantt-sink); 1 declared pure-kernel gap (time-scale). One interactive sub-lane (pan-zoom / input-handling).

The declared gaps 1

A gap is a piece the compiler knows it needs but doesn’t yet have a witnessed gift for — honest derive-first work, declared rather than hidden. The app is constructible; these are the parts you’d build.

tl.time-scale pure-kernel

time -> pixel-position: a deterministic linear map, a pure fold off the main pipe flow. Reclassified pipe->pure-kernel by the S13.1752 D1 ruling (drift 8251ecec). Declared derive-first gap; the time-scale witness gift (path b) is open gifts-8-reference-apps work.

Witness gifts 3

These shipped gifts witness stages of the pipeline — the real, downloadable parts the proof stands on.

timeline

range-filter

gantt-sink

The gifts are real and free; the wiring is a plan. Take the parts and build
the app — as the schematic reads it, a variation, or something we’d never
have compiled.

Built it, found a gap we missed, or think the wiring is
wrong? Shea wants to hear it — success stories, questions, and corrections all
land in the same inbox:

shea@loopmmt.com

---

# Search — Loop MMT
https://loopmmt.com/site/search.html
[coverage: standard · branch: root]

Search the corpus

One honest rank over the whole published site. An empty query browses the catalog; a query ranks it — grouped by coverage, every hit a link into the corpus.

This search exists because of a really kickass search system — the commission that asked for it, shown verbatim in Sweet Prompts.

Search index

---

# How This Site Is Made — Loop MMT
https://loopmmt.com/site/tag/how-this-site-is-made.html
[coverage: standard · branch: tag]

Referent

How This Site Is Made

2 pages on loopmmt.com represent this referent.

How This Site Is Made specimen

Sweet Prompts specimen

---
