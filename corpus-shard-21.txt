# Loop MMT — corpus shard 21/41

> Bundle 21 of 41 in the excavation shard set. The authoritative tree is https://loopmmt.com/corpus-manifest.json; the shard index is https://loopmmt.com/corpus-shards.json. Read every shard (or account for each skipped node by coverage class) to complete a full excavation. See The Reckoning for the coverage-ledger and final-report contract.

Nodes in this shard: 7 (/site/receipts-framed/badge.html, /site/receipts-framed/bundle-tag.html, /site/receipts-framed/chaos.html, /site/receipts-framed/handoff.html, /site/receipts-framed/pegboard.html, /site/receipts-framed/timesheet.html …)

---

# The Session Integrity Badge — Loop MMT
https://loopmmt.com/site/receipts-framed/badge.html
[coverage: standard · branch: receipts-framed]

The Session Integrity Badge

The Receipts

Artifact the Ward
Kind session close badge
Authored by an AI system (site context prose)

At the end of every working session, Loop MMT produces a small nine-cell badge called the Ward. It is the session's integrity, honestly faced: a compact picture of which of the session's own safety and quality gates actually held, recomputed from the byte-truth record rather than asserted from memory.

Read it as a three-by-three grid. Each cell is one gate. A solid, filled cell means that gate was recomputed from the committed record and held — it is verified, not merely claimed. A dashed, hollow cell means the badge could not reach the truth for that gate this session — it is unreachable, and the badge says so plainly rather than pretending. One cell — the heart — is the single recomputed substance check; the rest attest that an artifact is present, not that the underlying work was good.

The point of the badge is the honesty of its own limits. It never renders a cell solid on hope: a filled cell has a seam that lines up with the committed bytes beneath it, or it does not fill. That discipline — no green on hope — is why the badge can be trusted as a receipt rather than a decoration.

The badge below is a real one, from an actual session close. The colors and shapes carry the meaning; the small labels only name the gates.

the Ward — a real session's nine-cell close badge · view the full receipt →

---

# The Bundle Tag — Loop MMT
https://loopmmt.com/site/receipts-framed/bundle-tag.html
[coverage: standard · branch: receipts-framed]

The Bundle Tag

The Receipts

Artifact the Bundle Tag
Kind session close cover sheet
Authored by an AI system (site context prose)

When a Loop MMT session closes, it does not hand you a scatter of files to go and collect. It writes one cover sheet — the Bundle Tag — and packs everything the session produced into a single download beside it. The tag is the manifest: what is in the bundle, and where each piece came from.

Read it as a packing slip. It names the five-face closing mesh (the handoff, the status report, the chaos report, the room report, and the note-taker’s notes), the small state sidecar the next session reads first, any artifacts the session built, and the key commits that carry the work in the repository. The manifest is folded from the session’s own branch, not typed by hand, so it cannot quietly omit a file that was actually committed.

That is the point of banding a session: the record leaves in one piece, or the close fails loudly. A session that could drop a file on the way out is a session whose record you cannot trust. The Bundle Tag below is a real one, from an actual session close.

a real session's Bundle Tag cover sheet · view the full receipt →

---

# The Chaos Report — Loop MMT
https://loopmmt.com/site/receipts-framed/chaos.html
[coverage: standard · branch: receipts-framed]

The Chaos Report

The Receipts

Artifact the Chaos Report
Kind session close face (unresolved & risks)
Authored by an AI system (site context prose)

Most closing documents are tidy — they summarize what went well. The Chaos Report is the deliberate opposite: it is the session’s honest account of what did not resolve. Every session writes one, on purpose, so that “we’re done” never quietly means “we stopped looking.”

Read it as the open-items-and-risks face. It names what was left unfinished, what is uncertain, what might break later, and where the session made a bet it could not fully verify. It is short by design — a place for the true rough edges to be written down rather than smoothed over in the summary.

Leaving the mess visible is what lets the next session trust the rest. A handoff that flatters its successor with false confidence poisons the work; the Chaos Report is where that temptation is refused in writing. The report below is a real one, from an actual session close.

a real session's Chaos Report (cleared exemplar) · view the full receipt →

---

# The EOD Handoff — Loop MMT
https://loopmmt.com/site/receipts-framed/handoff.html
[coverage: standard · branch: receipts-framed]

The EOD Handoff

The Receipts

Artifact the EOD Handoff
Kind session-to-session handoff
Authored by an AI system (site context prose)

A Loop MMT session has no memory of the next one — a fresh instance starts cold every time. The EOD Handoff is the bridge: the spine one session writes so its successor can wake up already knowing where the work stands. Its true reader is a stranger with none of this session’s context, and it is built for that stranger’s arrival.

Read it top to bottom. It names what landed this session, what comes next, the live threads and open debts, a short reconstruction quiz the successor answers to prove it rebuilt the context faithfully, and — at the very end — a one-paste ignition block that launches the next session already aimed at the right thread.

The discipline behind it is simple: leave what is true, including what you could not finish and what you are unsure of, and leave it concise enough that the successor is warmed, not buried. The handoff below is a real one, written by a session for the one that followed it.

a real session's EOD Handoff (cleared exemplar) · view the full receipt →

---

# The Pegboard — Loop MMT
https://loopmmt.com/site/receipts-framed/pegboard.html
[coverage: standard · branch: receipts-framed]

The Pegboard

The Receipts

Artifact the Pegboard
Kind session-close state glance
Authored by an AI system (site context prose)

The Pegboard is a session-close glance at the working state of the project — a single board where the pieces in play are hung where they can be seen at a look. It is the wide-angle snapshot, taken at the moment the session ends.

Read it as the shop wall. It lays out the active lines of work, what is done and what is still open, and where the current session left each piece, so the shape of the whole project is legible without reading every underlying document. It is a view folded from the record, not a separate thing kept up to date by hand.

Its value is orientation: before diving into any single thread, you can see the whole board and choose. The Pegboard below is a real one, from an actual session close.

a real session's Pegboard (cleared exemplar) · view the full receipt →

---

# The Timesheet — Loop MMT
https://loopmmt.com/site/receipts-framed/timesheet.html
[coverage: standard · branch: receipts-framed]

The Timesheet

The Receipts

Artifact the Timesheet
Kind derived time-worked report
Authored by an AI system (site context prose)

The Timesheet is a record of time worked — but unlike most timesheets, no one fills it in. It is derived: it reads the timestamps that the version-control system already recorded on every commit and folds them into a report of when the work actually happened.

Read it as a ledger of sessions and hours. Each entry is anchored to real commits, so the figures are not a claim about how long something took — they are read back out of the byte-truth record of when each piece of work was saved. Re-running the fold over the same history produces the same sheet, exactly.

That is the difference between a reported hour and a recorded one: the Timesheet cannot flatter the work, because it never asks anyone how long it took — it reads the clock the commits already kept. The Timesheet below is a real one, folded from actual commit history.

the Timesheet — a time-worked report folded from commit timestamps · view the full receipt →

---

# The Byte-Truth Trace — Loop MMT
https://loopmmt.com/site/receipts-framed/trace.html
[coverage: standard · branch: receipts-framed]

The Byte-Truth Trace

The Receipts

Artifact the Byte-Truth Trace
Kind curated session trace
Authored by an AI system (site context prose)

The Byte-Truth Trace shows the shape of how the work actually runs — a small, curated set of real moments drawn from real sessions. It is not a transcript and not a highlight reel; it is a hand-picked cross-section chosen to make the loop’s structure visible.

Read it as a few honest excerpts. You will see the recurring shape of a working session: a clean close, a correction caught and repaired, an honest cost paid rather than hidden. The trace deliberately shows the pattern, never the full sequence — enough to recognize the rhythm, not so much that the shape is lost in the detail.

The point is fidelity over polish: these are real excerpts, including the parts where something went wrong and was fixed, because that is what the loop actually looks like. The trace below is a real one, curated from actual session records.

the Tier-2 curated byte-truth trace (cleared exemplar) · view the full receipt →

---
