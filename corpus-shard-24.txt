# Loop MMT — corpus shard 24/60

> Bundle 24 of 60 in the excavation shard set. The authoritative tree is https://loopmmt.com/corpus-manifest.json; the shard index is https://loopmmt.com/corpus-shards.json. Read every shard (or account for each skipped node by coverage class) to complete a full excavation. See The Reckoning for the coverage-ledger and final-report contract.

Nodes in this shard: 3 (/site/receipts-framed/badge.html, /site/receipts-framed/bundle-tag.html, /site/receipts-framed/chaos.html)

---

# The Session Integrity Badge — Loop MMT
https://loopmmt.com/site/receipts-framed/badge.html
[coverage: standard · branch: receipts-framed]

← The Receipts

The Session Integrity Badge

The Receipts

Artifact the Ward
Kind session close badge
Authored by an AI system (site context prose)

At the end of every working session, Loop MMT produces a small nine-cell badge called the Ward. It is the session's integrity, honestly faced: a compact picture of which of the session's own safety and quality gates actually held, recomputed from the byte-truth record rather than asserted from memory.

Read it as a three-by-three grid. Each cell is one gate. A solid, filled cell means that gate was recomputed from the committed record and held — it is verified, not merely claimed. A dashed, hollow cell means the badge could not reach the truth for that gate this session — it is unreachable, and the badge says so plainly rather than pretending. One cell — the heart — is the single recomputed substance check; the rest attest that an artifact is present, not that the underlying work was good.

The point of the badge is the honesty of its own limits. It never renders a cell solid on hope: a filled cell has a seam that lines up with the committed bytes beneath it, or it does not fill. That discipline — no green on hope — is why the badge can be trusted as a receipt rather than a decoration.

The badge below is a real one, from an actual session close. The colors and shapes carry the meaning; the small labels only name the gates.

This artifact's siblings: Ward, the runnable gift · the prompt that made it

A Session's Integrity Badge — Receipts — Loop MMT

the Ward

open
CX
band
Currency
heart
KX
confirm
SX
manifest
verified 5 · declared 0 · unreachable 4
attests artifact presence · the heart is the one recomputed substance check

the Ward — a real session's nine-cell close badge · view the full receipt →

---

# The Bundle Tag — Loop MMT
https://loopmmt.com/site/receipts-framed/bundle-tag.html
[coverage: standard · branch: receipts-framed]

← The Receipts

The Bundle Tag

The Receipts

Artifact the Bundle Tag
Kind session close cover sheet
Authored by an AI system (site context prose)

When a Loop MMT session closes, it does not hand you a scatter of files to go and collect. It writes one cover sheet — the Bundle Tag — and packs everything the session produced into a single download beside it. The tag is the manifest: what is in the bundle, and where each piece came from.

Read it as a packing slip. It names the five-face closing mesh (the handoff, the status report, the chaos report, the room report, and the note-taker’s notes), the small state sidecar the next session reads first, any artifacts the session built, and the key commits that carry the work in the repository. The manifest is folded from the session’s own branch, not typed by hand, so it cannot quietly omit a file that was actually committed.

That is the point of banding a session: the record leaves in one piece, or the close fails loudly. A session that could drop a file on the way out is a session whose record you cannot trust. The Bundle Tag below is a real one, from an actual session close.

Bundle Tag — Loop MMT™

Day Bundle — session [session]

Closed off-floor close, committed to canonical main.

State in one line. Obstacle-block feature (mechanics + visuals) SHIPPED to main; 3D build card reconciled to byte-truth; remaining 3D work = full element set in 3D + generator productionization + delight + integration.

Five-face mesh

eod-handoff-[session].md — handoff spine (9 sections + Champollion + For My Successor + Ignition Block)

status-report-[session].md — project status snapshot

chaos-report-[session].md — unresolved / risks / chaos lens

room-report-[session].md — the working-session narrative

bevs-notes-[session].md — closing voice

the STATE sidecar (six slots; separate gate)

Session artifacts (on main)

the shipped feature's render + instancing + textures

the feature's data library (144 puzzles)

the feature's content generator

the reconciled launch/build card

Key commits

35aa7b64 obstacle mechanics · 40b11243 visual pass · c5dc55c1 card reconcile

Next: productionize the general 3D generator, then render the full element set in 3D. Read the build card first.

a real session's Bundle Tag cover sheet · view the full receipt →

---

# The Chaos Report — Loop MMT
https://loopmmt.com/site/receipts-framed/chaos.html
[coverage: standard · branch: receipts-framed]

← The Receipts

The Chaos Report

The Receipts

Artifact the Chaos Report
Kind session close face (unresolved & risks)
Authored by an AI system (site context prose)

Most closing documents are tidy — they summarize what went well. The Chaos Report is the deliberate opposite: it is the session’s honest account of what did not resolve. Every session writes one, on purpose, so that “we’re done” never quietly means “we stopped looking.”

Read it as the open-items-and-risks face. It names what was left unfinished, what is uncertain, what might break later, and where the session made a bet it could not fully verify. It is short by design — a place for the true rough edges to be written down rather than smoothed over in the summary.

Leaving the mess visible is what lets the next session trust the rest. A handoff that flatters its successor with false confidence poisons the work; the Chaos Report is where that temptation is refused in writing. The report below is a real one, from an actual session close.

Home
The Thesis
The Case
Timeline
Corpus
The Apps
Sweet Prompts
Discoveries
The Threads
Gifts
Knowledge
Who

Receipt · a near-miss log

What the system almost got wrong

A real session's chaos report, lightly cleaned. One item is redacted in the open.

Every session keeps a log of its own near-misses — the traps it caught, the risks it surfaced, the wrong turns it corrected. A receipt that names its own mistakes proves honesty better than any polished claim. This is one, exactly as written, with a single item cut where the cut is the point.

Traps caught (byte-truth over the handoff)

1. corpus.html is GENERATED, not static. The doc-index lived only in a hand-edit — the first deploy would have silently wiped the whole Documents hub. Fixed by baking the doc-index into the builder. Highest-severity catch of the session.

2. Deploy is air-gapped GitHub Pages, not an app server. An inherited note described an app-server deploy pattern that was byte-wrong for the website. Real path: the static deploy to the public Pages repo.

3. creed.html leaked an internal provenance path (dev-comment) that refused the whole-tree predeploy gate — pre-existing, unrelated to the hub, but blocked deploy. Scrubbed.

4. rsync not installed — the deploy failed (exit 127) until it was installed. Not persistent across container resets.

5. No git identity on the public clone — commit failed until name/email set on the deploy clone.

6. Dark-tab favicon regression (self-introduced): the new ink loop vanishes on dark browser tabs on a transparent favicon. Fixed with a paper chip.

■ one item redacted — guard G2

████████████████████████████████████████   ██████████████████████████████

Here the session surfaced a real, honest open risk for the operator — a note about the credential and air-gap setup. It's a genuine near-miss, flagged twice, not auto-actioned. But its shape is exactly the kind of thing the receipts never publish, so the whole item is cut, not paraphrased. The cut is the receipt.

Decisions

· Operator pick C on the two standalone HTML docs (strip content, rebuild) — carded, lints PASS, resolved.

· Choice A on deploy (push from this env) — carded with the honest air-gap trade-off; operator chose A twice.

· Logo split (loop=ink / bit=persimmon) — executed per the site's own scheme; flip offered.

The traps are verbatim. The one cut item is removed in full because publishing the shape of that risk would itself be the leak — the honest way to show a near-miss log is to keep every operational trap and black out the one whose shape is sensitive.
← back to the receipts

a real session's Chaos Report (cleared exemplar) · view the full receipt →

---
